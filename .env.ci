# Placeholder env for CI (build + typecheck). Shape-valid, NOT real secrets.
# Real values are only needed for live local `pnpm dev`. Never put real keys here.
STEAM_API_KEY=ci_placeholder_key
STEAM_ID=76561190000000000
DATABASE_URL=file:./ci.db
CRON_SECRET=ci_placeholder_secret
