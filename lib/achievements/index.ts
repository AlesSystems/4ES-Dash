/**
 * Barrel re-export for lib/achievements/aggregate.ts.
 * All exports are named (no default exports).
 */

export type { MergedAchievement, GameAchievements, LibrarySummary } from './aggregate';
export { mergeGameAchievements, aggregateLibrary } from './aggregate';
